/**
 * This component renders nothing and is only used when the chat with the
 * chatbot ends.
 *
 * @returns {JSX.Element} An empty fragment.
 */
export const Terminate: React.FC = (): JSX.Element => {
  return <></>;
};
